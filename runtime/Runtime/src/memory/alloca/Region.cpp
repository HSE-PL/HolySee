#include "Region.hpp"

// template <have_size T>
// Region<T>::Region(const size_t start_region,
//                   const size_t count_items,
//                   const size_t t_size)
//     : start(start_region), count(count_items),
//       size(count * t_size), t_size(t_size),
//       items() {
//
//   for (int i = 0; i < count_items;
//        items.push_back(T(
//            t_size, start_region + t_size *
//            i++)))
//     ;
// }
